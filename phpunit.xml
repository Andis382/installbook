<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="FILESYSTEM_DISK" value="local"/>

        <!--
            Defaults to SQLite in memory, which needs no setup. These are not forced, so
            exporting DB_CONNECTION / DB_DATABASE before running the suite wins: on a PHP
            build without pdo_sqlite, run
              DB_CONNECTION=pgsql DB_DATABASE=installbook_test php artisan test
        -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>

        <!-- Messages are recorded, never sent, and plate reading is off. -->
        <env name="INSTALLBOOK_MESSAGING_DRIVER" value="manual"/>
        <env name="INSTALLBOOK_OCR_DRIVER" value="none"/>
        <env name="INSTALLBOOK_DEFAULT_LOCALE" value="sq"/>
        <env name="INSTALLBOOK_COUNTRY_PREFIX" value="355"/>
    </php>
</phpunit>
